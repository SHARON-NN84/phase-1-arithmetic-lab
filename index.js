// Define num1 and num2 so we can multiply them
const num1 = 2;
const num2 = 31;

// Multiply num1 and num2 to get 62
const multiply = num1 * num2;

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;

// Define num3 and num4 to get a remainder of 4
const num3 = 28;
const num4 = 6;

// Use the modulo operator to get the remainder
const mod = num3 % num4;

// Use Math.max to find the highest number in the set
const max = Math.max(1, 3, 5, 10, 15, 20);
